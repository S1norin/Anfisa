/**
 * Schema view (issue #13): low-clutter orthographic views, dimension lines,
 * frusta, angle arcs, PNG export. Placeholder for the scaffold.
 */
export function SchemaView() {
  return (
    <div className="view view-schema" data-testid="schema-view">
      <h2>Schema</h2>
      <p data-testid="schema-placeholder">
        Dimensioned schema view lands here in issue #13.
      </p>
    </div>
  );
}
