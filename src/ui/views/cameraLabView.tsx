/**
 * Camera Lab (issue #12): select one camera + one parcel, freeze time, edit
 * optics and report FOV/PPM/blur/angle. Placeholder for the scaffold.
 */
export function CameraLabView() {
  return (
    <div className="view view-camera-lab" data-testid="camera-lab-view">
      <h2>Camera Lab</h2>
      <p data-testid="camera-lab-placeholder">
        Camera + parcel editor lands here in issue #12.
      </p>
    </div>
  );
}
